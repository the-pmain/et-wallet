import { EmailConversationsList } from '@/features/email-manager/ui/EmailConversationsList'

/** Conversation list for the email manager. */
export function EmailConversationsPage() {
  return <EmailConversationsList />
}
