import { EmailNewConversation } from '@/features/email-manager/ui/EmailNewConversation'

/** Compose the first message in a new conversation. */
export function EmailNewConversationPage() {
  return <EmailNewConversation />
}
