# Технический долг

Реестр отложенных задач. Ведётся по ходу разработки, чтобы финальный аудит
получил готовую повестку, а не собирался по памяти.

**Правило ведения.** Сюда попадает то, стоимость чего растёт линейно.
Уязвимости, ошибки в контрактах, на которые опираются следующие этапы,
и блокирующие дефекты исправляются немедленно и в этот список не заносятся.

Статусы: `открыт` · `в работе` · `закрыт` · `отклонён`

---

## Безопасность

| № | Задача | Этап | Приоритет | Статус |
| --- | --- | --- | --- | --- |
| S-1 | Сузить `connect-src` до конкретных RPC-доменов. Буквально невыполнимо: перечень на этапе сборки отменяет возможность указать свой узел — главную защиту приватности запросов. Сделано иначе (этап 26): перечень задаётся переменной `VITE_CSP_CONNECT_SRC` тем, кто размещает сборку, и попадает и в метатег, и в заголовки. По умолчанию обмен прежний | 1 | **высокий** | **закрыт (этап 26)** — решение передано размещающему |
| S-2 | Пользовательский RPC-адрес для встроенных сетей. До его появления приватность запросов не обеспечена: оператор публичного узла видит IP и все обращения | 3 | **высокий** | **закрыт (этап 12.1)** |
| S-29 | Alchemy по умолчанию сосредоточивает у одного оператора IP и все адреса каждого пользователя. Публичные узлы хотя бы разносят запросы между несколькими независимыми операторами. Нужен явный выбор источника при первом запуске, а не молчаливое умолчание | 12.1 | **высокий** | открыт |
| S-30 | Ключ Alchemy попадает в бандл открытым текстом (свойство любого клиентского приложения). Требуется ограничение ключа доменом в панели оператора и проверка этого требования при сборке | 12.1 | **высокий** | открыт |
| S-31 | `connect-src` в CSP по-прежнему разрешает любой HTTPS, и сузить его нельзя, пока пользователь вправе добавить произвольный RPC-адрес. Нужен список доменов, формируемый из конфигурации и пользовательских адресов, — что для meta-тега недостижимо и требует заголовка от хостинга. Уточняет S-1 | 12.1 | средний | открыт |
| S-32 | Отказ `sendRawTransaction` при отказе транспорта оставляет судьбу транзакции неизвестной. Повтор на другом узле запрещён сознательно; нужен экран, объясняющий неопределённость и предлагающий проверить хэш в обозревателе | 12.1 | средний | открыт |
| S-35 | Индексатор получает адрес пользователя и возвращает всю его финансовую историю разом: размер портфеля, контрагентов, время каждой операции. Это заметно больше того, что видит обычный RPC-узел. Нужен явный вопрос при первом включении, а не молчаливое умолчание по наличию ключа | 14 | **высокий** | открыт |
| S-36 | Символ токена ничем не проверяется | 14 | **высокий** | **частично (этап 16)** — добавленные токены помечены «непроверенный»; курируемого списка нет, см. S-38 |
| S-38 | Встроенного списка проверенных токенов нет: вписать адреса известных контрактов по памяти значит рискнуть пометить подделку как настоящую. Нужен список, сверенный с авторитетным источником, и признак проверенности в `IToken` | 16 | **высокий** | **закрыт (этап 28)** — встроенный список из двадцати двух контрактов семи сетей, значения измерены живым опросом узлов |
| S-39 | Логотипы токенов не загружаются: внешний источник заблокирован CSP, раскрывает состав портфеля и делает подделку убедительной. Знак выводится из адреса контракта. Если логотипы понадобятся, единственный приемлемый путь — встроенный набор в сборке для проверенного списка | 16 | низкий | открыт |
| S-40 | Получатель не проверяется на то, что он контракт | 17 | **высокий** | **закрыт (этап 20)** — `eth_getCode` на шаге подтверждения, отдельное предупреждение |
| S-41 | Nonce берётся с учётом очереди (`pending`), но между подготовкой и отправкой пользователь может подписать транзакцию в другом окне того же кошелька. Совпадение nonce приведёт к вытеснению одной операции другой без объяснения. Нужен реестр выданных nonce на время жизни сессии | 17 | средний | открыт |
| S-33 | Отпечаток адреса строится свёрткой FNV-1a: подобрать адрес с той же картинкой вычислительно несложно. Для защиты от целенаправленной подмены нужен отпечаток на криптографической хэш-функции с большим числом различимых состояний | 13 | средний | открыт |
| S-34 | Заголовок и панель навигации остаются видимыми при потере фокуса окном. Имя аккаунта и сеть видны наблюдателю; в `index.css` оставлен якорь под скрытие содержимого вместе с автоблокировкой (S-26) | 13 | низкий | открыт |
| S-3 | Заменить `getPrivateKeyForSigning` подписью внутри модуля. Тогда приватный ключ перестанет быть значением, которое где-то передаётся | 5 | **высокий** | **закрыт (этап 9)** |
| S-17 | Разбор опасных шаблонов EIP-712 и предупреждение перед подписью | 9 | **высокий** | **закрыт (этап 21)** — `findDappRisks` распознаёт `Permit`, Permit2 и неограниченную сумму |
| S-18 | Проверка `domain.verifyingContract` | 9 | **высокий** | **частично (этап 21)** — отсутствие контракта помечается; сверки со списком известных нет, см. S-51 |
| S-19 | Показ разобранной структуры EIP-712 вместо хэша | 9 | средний | **закрыт (этап 21)** — экран запроса показывает домен, тип и поля |
| S-20 | Предупреждение при подписи сообщения, похожего на транзакцию | 9 | низкий | **закрыт (этап 21)** |
| S-51 | Списка известных контрактов для сверки `verifyingContract` нет. Пользователь видит адрес, которому предназначена подпись, но отличить настоящий Permit2 от подставного не может. Уточняет S-18 и требует того же курируемого списка, что и S-38 | 21 | **высокий** | открыт |
| S-52 | Подпись по запросу приложения не спрашивает пароль, хотя отправка из кошелька спрашивает (настройка `confirmBeforeSigning`). Удалённый запрос опаснее собственного действия, а защищён слабее | 21 | **высокий** | **закрыт (этап 26)** — пароль спрашивается по той же настройке `confirmBeforeSigning`, что и при отправке из кошелька |
| S-53 | Транзакция от приложения проходит через `TransactionService.prepare`, который заменяет присланный лимит газа собственной оценкой. Для вызова контракта оценка может отличаться от нужной, и приложение получит отказ вместо выполнения. Лимит от приложения следует учитывать, показывая расхождение пользователю | 21 | средний | открыт |
| S-54 | Разрешения, выданные приложениям (`approve`), нигде не перечисляются: узнать, какие контракты имеют доступ к токенам, и отозвать их из кошелька нельзя | 21 | средний | **закрыт (этап 27)** — список действующих разрешений с проверкой в контракте и отзывом транзакцией |
| S-4 | HTTP-заголовки на стороне хостинга: `Content-Security-Policy`, `X-Frame-Options: DENY`. Meta-тег не поддерживает `frame-ancestors` | 1 | средний | открыт |
| S-5 | Отказ от `'unsafe-inline'` в `style-src`. Требует отказа от inline-стилей Radix UI | 1 | средний | открыт |
| S-6 | Подмена встроенной сети в хранилище должна быть событием безопасности для пользователя, а не только предупреждением в журнале | 3 | средний | открыт |
| S-37 | Адрес обозревателя пользовательской сети задаёт тот, кто её добавил, и переход по нему из кошелька ведёт на произвольный сайт. Форма предупреждает, но ссылка в истории операций ничем не помечена — нужен признак «внешний непроверенный адрес» рядом с самой ссылкой | 15 | средний | открыт |
| S-7 | Тест: секреты не попадают в журнал. `NullLogger` накапливает записи именно для этого | 3, 4 | средний | открыт |
| S-8 | Проверка стойкости парольной фразы BIP-39 («25-е слово»). Сейчас принимается любая, включая пустую | 4 | средний | открыт |
| S-9 | Журнал экспортов стирается вместе с хранилищем при сбросе. Выданный секрет остаётся выданным, а предупреждение исчезает | 5 | низкий | открыт |
| S-12 | Ограничение числа попыток разблокировки и задержка между ними. Стойкость KDF замедляет перебор, но не отменяет необходимости ограничения на уровне приложения | 7 | **высокий** | **закрыт (этап 26)** — растущая задержка после трёх неудач, счётчик в незашифрованных настройках переживает перезагрузку |
| S-13 | Политика сложности пароля. Сейчас принимается любой, включая пустой | 7 | **высокий** | **закрыт (этап 11)** |
| S-21 | Список часто встречающихся паролей содержит десятки записей вместо десятков тысяч. Полный список (например, из подборок утечек) весит мегабайты и требует загрузки отдельным ресурсом либо фильтра Блума | 11 | средний | открыт |
| S-22 | Проверка пароля по базе утечек через k-анонимность (Have I Been Pwned). Требует сетевого запроса на экране создания кошелька — обсудить приемлемость: даже префикс хэша сообщает стороннему сервису факт создания кошелька | 11 | средний | открыт |
| S-23 | Подтверждение seed-фразы проверяет три слова из двенадцати. Пользователь, записавший фразу с ошибкой в непроверенном слове, узнает об этом при восстановлении | 11 | средний | открыт |
| S-24 | Копирование в буфер обмена не очищается по таймеру | 11 | средний | **закрыт (этап 21)** — `copyWithAutoClear` применён ко всем местам, где кошелёк копирует что-либо: адрес получения и приватный ключ |
| S-45 | Копирование seed-фразы на экране создания идёт мимо `copyWithAutoClear`: фраза остаётся в буфере обмена до следующего копирования. Это опаснее адреса — фраза даёт полный доступ к средствам | 20 | **высокий** | **закрыт (этап 21) иначе, чем предполагалось** — кнопки копирования фразы нет ни на одном экране. `SeedPhraseDisplay` принимает необязательный `onCopy`, но ни один вызывающий его не передаёт. Добавить кнопку и подключить к ней автоочистку значило бы создать новый путь утечки под видом закрытия долга: буфер обмена читают другие приложения, а фразу, в отличие от пароля, сменить нельзя |
| S-46 | Автоблокировка отсчитывает время только при открытой вкладке: `setInterval` в фоновой вкладке замедляется браузером, а закрытая вкладка не считает вовсе. Кошелёк, оставленный в фоне на час, может заблокироваться позже обещанного. Нужна сверка по метке времени при возврате фокуса | 20 | средний | открыт |
| S-47 | Подтверждение паролем перед подписью не ограничивает число попыток: подобрать пароль через эту форму можно с той же скоростью, что и через экран разблокировки. Связано с S-12 | 20 | средний | **закрыт (этап 26)** — тот же ограничитель, что у входа: разные счётчики означали бы, что подбирающий выберет форму без ограничения |
| S-48 | Обезвреживание недоверенного текста применено к списку токенов; имена сетей, адреса обозревателей и тексты системных уведомлений по-прежнему показываются без обработки | 20 | средний | открыт |
| S-49 | `DangerConfirm` создан, но подключён не везде: удаление сети, удаление токена и отзыв согласия на курсы по-прежнему выполняются без единого подтверждения | 20 | средний | открыт |
| S-50 | Trusted Types включены только в боевой сборке: dev-сервер Vite использует inline-скрипты, и политика ломала бы разработку. Нарушение, внесённое в dev, обнаружится только при сборке | 20 | низкий | открыт |
| S-25 | Проверка на нулевой буфер в `getRandomBytes` не действует на запросах короче 16 байт: на такой длине ноль — обычное значение исправного генератора. Полноценная проверка потребовала бы пула энтропии, выдающего короткие значения из длинного запроса | 11 | низкий | открыт |
| S-26 | Автоблокировка по бездействию | 12 | **высокий** | **закрыт (этап 20)** — `AutoLockService`, предупреждение за минуту, срок настраивается |
| S-27 | Опрос балансов раскрывает оператору публичного узла IP и все адреса пользователя, связывая их между собой. Панель обращается к узлу регулярно, поэтому S-2 (пользовательский RPC) стал заметно важнее | 12 | **высокий** | открыт |
| S-28 | Список аккаунтов на экране показывает адреса без подтверждения. Наблюдатель за спиной пользователя получает связку всех его адресов; нужен режим скрытия сумм и адресов | 12 | низкий | открыт |
| S-14 | Перешифровка хранилища при обнаружении устаревших параметров KDF. `needsUpgrade` реализован, автоматическое применение при разблокировке — нет | 7 | средний | открыт |
| S-15 | Argon2id как альтернатива PBKDF2. Устойчивее к атаке на GPU за счёт требований к памяти; требует WASM-реализации. Формат контейнера уже поддерживает | 7 | средний | открыт |
| S-16 | Проверка защищённого контекста при запуске. Сейчас отсутствие `crypto.subtle` обнаруживается только при первой операции шифрования | 7 | низкий | открыт |
| S-10 | Ограничение числа пользовательских сетей — защита от засорения хранилища | 3 | низкий | открыт |
| S-11 | Subresource Integrity, если появятся внешние ресурсы. Сейчас их нет и лучше, чтобы не появлялись | 1 | низкий | открыт |

## Архитектура

| № | Задача | Этап | Приоритет | Статус |
| --- | --- | --- | --- | --- |
| A-1 | `using` вместо ручного `wipe()` в `finally` | 4, 5 | **высокий** | **частично (этап 20)** — `withSecret`/`withSecretSync` дают ту же гарантию без нового синтаксиса; применены в подписи импортированным ключом, остальные места ещё на `try/finally` |
| A-89 | Оставшиеся места ручного затирания (вывод HD-кошелька, экспорт ключа, онбординг) не переведены на `withSecret`. Пока они работают верно, но правило по-прежнему держится на внимательности | 20 | средний | открыт |
| A-2 | Обсудить `Result<T, E>` вместо исключений в `core`. Сделает обработку ошибок обязательной на уровне типов. Крупное решение, затрагивает каждый метод ядра | 2 | средний | открыт |
| A-3 | Правило ESLint против приведений вида `as Address`, `as ChainId` вне валидирующих конструкторов. Сейчас брендирование обходится приведением типа | 2 | средний | открыт |
| A-4 | Проверка отсутствия циклов между модулями внутри `core`. Границы между слоями контролируются, внутри слоя — нет | 2 | средний | открыт |
| A-5 | Тесты типов через `expectTypeOf`. Позволят проверять сами контракты — например, что `IAccount` не содержит поля с приватным ключом | 2 | низкий | открыт |
| A-6 | Автоопределение соглашения о пути деривации при импорте: вывести адреса по обеим схемам и предложить ту, где есть история | 5 | низкий | открыт |
| A-7 | Обратный поиск индекса по адресу — понадобится при обработке входящих транзакций | 5 | низкий | открыт |
| A-8 | Вычисление адреса контракта: CREATE (требует RLP) и CREATE2 (EIP-1014). Отложено до этапа транзакций, где RLP понадобится для сериализации — иначе кодировщик пришлось бы писать дважды | 6 | средний | открыт |
| A-9 | EIP-1191: контрольная сумма адреса с учётом chainId. Применяется в RSK и нескольких форках; для поддерживаемых сетей не требуется | 6 | низкий | открыт |
| A-10 | Расширить список адресов сжигания. Сейчас только нулевой и `0x…dEaD`. Эвристика намеренно узкая: ложное срабатывание заставит отменить законный перевод | 6 | низкий | открыт |
| A-11 | `baseFeePerGas` восстанавливается из `maxFeePerGas` обратным преобразованием формулы ethers — это оценка, а не значение из заголовка блока. Запрашивать блок напрямую при появлении экрана детальной настройки комиссии | 8 | средний | открыт |
| A-12 | Периодическая сверка chainId у уже установленного соединения. Сейчас проверка выполняется только при подключении; узел может сменить сеть позже | 8 | средний | открыт |
| A-13 | Поддержка WebSocket-транспорта. `RpcClient.attach` уже принимает готовый провайдер ethers — точка расширения существует | 8 | низкий | открыт |
| A-33 | Два публичных узла Ethereum из трёх (`eth.llamarpc.com`, `cloudflare-eth.com`) не отвечают из браузера — вероятно, отклоняют кросс-доменный запрос. Перебор это скрывает, но резерв фактически отсутствует. Перепроверить список публичных узлов всех сетей именно из браузера | 12.1 | **высокий** | **закрыт (этап 28)** — мёртвые узлы убраны, списки пересобраны по измерению на живых узлах |
| A-34 | `ProviderPool` и `RpcManager` решают пересекающиеся задачи. Пул сохранён как минимальная реализация `IProviderResolver`, но после появления настроек RPC приложение использует только менеджер — стоит либо удалить пул, либо свести его к общей основе | 12.1 | средний | открыт |
| A-35 | Выдержка после отказа узла хранится в памяти менеджера и теряется при блокировке кошелька. Отказавший узел будет опрошен заново сразу после разблокировки | 12.1 | низкий | открыт |
| A-36 | Проверка доступности не измеряет качество узла, кроме задержки: не сверяет высоту блока с остальными и не выявляет узел, отставший от сети. Отставший узел отдаёт устаревший nonce и приводит к отклонению транзакции | 12.1 | средний | открыт |
| A-37 | Порядок источников RPC не настраивается пользователем: он задан в composition root. После появления экрана настроек стоит дать выбор — например, отключить Alchemy полностью | 12.1 | низкий | открыт |
| A-38 | Логотип непрозрачен и тяжёл; внутри приложения используется векторный знак | 13 | средний | **закрыт (этап 13)** — получены прозрачные исходники, набор размеров готовит `npm run icons` |
| A-46 | Значок 512×512 (161 КБ) генерируется и попадает в сборку, но пока никем не запрашивается: он нужен манифесту веб-приложения и расширения, которых ещё нет | 13 | низкий | открыт |
| A-47 | Исходники логотипа хранятся растром 1024×1024. Векторный источник (SVG) дал бы чёткость на любом размере и вес в единицы килобайт вместо сотен | 13 | низкий | открыт |
| A-39 | Выбор оформления не сохраняется между сессиями: `ThemeProvider` держит его в состоянии React. Постоянное хранилище появится вместе с IndexedDB (A-20) | 13 | низкий | открыт |
| A-40 | Переключение аккаунта доступно только из настроек, хотя шапка со стрелкой вниз подразумевает выпадающий список. Нужен переключатель прямо в шапке | 13 | средний | открыт |
| A-41 | Экраны Assets и NFT — готовый каркас без источника данных. Подключение сведётся к замене пустого состояния списком, но до тех пор разделы существуют ради навигации, а не ради содержимого | 13 | **высокий** | **закрыт (этап 26)** — раздел NFT получил источник данных; активы работали с этапа 14 |
| A-42 | Живой фон рисуется тремя градиентными слоями во весь экран плюс тринадцать монет. На слабых машинах это стоит кадров даже при анимации только трансформации; нужен замер и, при необходимости, уменьшение числа слоёв на узких экранах. С появлением приглушённого фона за панелью кошелька слои живут ещё и всё время работы, а не только на экранах входа | 13 | средний | открыт |
| A-44 | Уходящий экран при переходе исчезает мгновенно: анимируется только появление нового. Полноценная смена с обеих сторон требует либо удержания обоих экранов в дереве, либо View Transitions API — второе доступно не во всех браузерах и не проверено в связке `HashRouter` + `<Routes>` | 13 | низкий | открыт |
| A-45 | Полный блок логотипа непригоден для тёмной темы: надпись «Wallet» набрана тёмно-синим (rgb 50, 54, 75) и на фоне rgb 38, 33, 48 неразличима. В интерфейсе используется знак без надписи; полный блок остаётся в `brand/` для витрины и документов. Для тёмных поверхностей нужен вариант надписи в светлом начертании | 13 | низкий | открыт |
| A-43 | Шрифт Inter собирается со всеми подмножествами Unicode (7 файлов, около 220 КБ на диске). Браузер скачивает только латиницу и кириллицу (67 КБ), но неиспользуемые файлы всё равно попадают в сборку и в пакет расширения | 13 | низкий | открыт |
| A-14 | Поддержка EIP-2930 (список доступа). Тип объявлен в `TRANSACTION_TYPE` и проходит через подпись, но поле `accessList` в `ISignableTransaction` отсутствует | 9 | низкий | открыт |
| A-15 | Подпись аппаратными кошельками. `IKeyring` предусматривает асинхронную подпись, `SigningService` синхронен — при подключении Ledger потребуется асинхронный вариант | 9 | средний | открыт |
| A-16 | Обнаружение занятых адресов при восстановлении кошелька (gap limit). Сейчас после импорта seed-фразы аккаунты создаются вручную по одному; кошелёк с двадцатью адресами придётся восстанавливать двадцатью нажатиями | 10 | **высокий** | **закрыт (этап 28)** — поиск по правилу BIP-44 с промежутком в двадцать адресов: один раз после восстановления и по кнопке в настройках |
| A-17 | Импорт наблюдаемого адреса и аппаратного кошелька. `KEYRING_TYPE` их предусматривает, `AccountManager` пока работает с HD и импортированными ключами | 10 | средний | открыт |
| A-18 | Атомарность операций с аккаунтами. Удаление пишет ключ и метаданные последовательно; `IStorageService.transaction` существует, но здесь не задействован | 10 | средний | открыт |
| A-19 | Пакетное создание аккаунтов. Сейчас `create()` пишет в хранилище на каждый вызов | 10 | низкий | открыт |
| A-20 | Хранилище приложения работает в памяти: после перезагрузки вкладки кошелёк снова не инициализирован. Постоянное хранилище (IndexedDB, затем `chrome.storage.local`) подменяется одной строкой в `createAppServices` | 11 | **высокий** | **закрыт (этап 26)** — `IndexedDbStorageService`: данные переживают перезагрузку, схема версионируется, транзакции откатываются самой базой |
| A-21 | `OnboardingService` не связан с `AccountManager` и `HDWalletService` | 11 | **высокий** | **закрыт (этап 12)** |
| A-25 | Истории входящих переводов нет | 12 | **высокий** | **закрыт (этап 14)** — два источника: разбор журналов и индексатор |
| A-48 | **Публичные узлы непригодны для разбора журналов.** Замер на живых узлах: все точки `publicnode` (наши по умолчанию во всех сетях) отвечают «Please specify an address in your request» на выборку без указания контракта — а именно она нужна для поиска всех токенов сразу. `drpc.org` запрос принимает, но после серии обращений отвечает «Can't route your request to suitable provider». Итог: без ключа индексатора история чаще всего недоступна. Нужен либо список узлов, поддерживающих такие выборки, либо собственный узел | 14 | **высокий** | **частично закрыт (этап 28)** — история работает на публичных узлах в Ethereum, Optimism, Base и Avalanche; Polygon, Arbitrum и BNB Chain по-прежнему требуют своего узла либо ключа |
| A-49 | Символ и число знаков токена при разборе журналов неизвестны | 14 | **высокий** | **частично (этап 16)** — `TokenService` умеет их читать; подстановка в записи истории вынесена в A-63 |
| A-63 | История переводов не подставляет метаданные отслеживаемых токенов: `TokenService.get` возвращает символ и число знаков, но `HistoryService` его не спрашивает. Переводы токенов по-прежнему показываются в необработанных единицах | 16 | **высокий** | **закрыт (этап 26)** — метаданные отслеживаемых токенов подставляются в записи истории |
| A-50 | Разбор `TransferBatch` упрощён: читаются длины и последовательные элементы, как их формирует эталонная реализация ERC-1155. Контракт, разместивший массивы в ином порядке, будет разобран неверно. Нужен полноценный декодер ABI | 14 | средний | открыт |
| A-51 | История не постраничная: запрашиваются первые 50 записей, показать более ранние нельзя. Индексатор отдаёт `pageKey`, разбор журналов — окно блоков; оба поддерживают продолжение | 14 | средний | открыт |
| A-52 | Время включения в блок при разборе журналов неизвестно: показывается номер блока. Запрашивать заголовок на каждую запись — десятки лишних обращений; нужен кэш заголовков | 14 | низкий | открыт |
| A-53 | История не кэшируется между переключениями сети и аккаунта: каждый возврат перезапрашивает всё заново | 14 | средний | открыт |
| A-71 | Отбор и поиск работают только по загруженной части истории. Постраничная подгрузка добавлена (этап 26): кнопка «Load earlier» продолжает выдачу того же источника, а пустой результат отбора при незагруженном остатке прямо оговаривает, что об этом остатке ничего не сказано | 16 | **высокий** | **закрыт (этап 26)** |
| A-72 | Отбора по периоду нет. Часть записей приходит без времени включения в блок (`timestamp: null`, см. A-52), и отбор по дате пришлось бы применять к неполным данным: запись без времени либо исчезала бы из любого периода, либо попадала во все. Нужен кэш заголовков блоков | 16 | средний | открыт |
| A-73 | Поиск по символу токена работает только по записям, где символ известен. Разбор журналов его не сообщает (см. A-63), поэтому запрос «USDC» не найдёт переводы USDC, полученные этим источником | 16 | средний | открыт |
| A-74 | Условия отбора сбрасываются при уходе с экрана и при переключении сети либо аккаунта. Для переключения это верно — записи относятся к другому аккаунту, — но возврат из обозревателя блоков тоже теряет отбор | 16 | низкий | открыт |
| A-75 | Поиск выполняется на каждое нажатие клавиши по всему массиву. При 50 записях это незаметно, но после появления постраничной подгрузки потребуется задержка ввода | 16 | низкий | открыт |
| A-26 | Балансы токенов ERC-20 | 12 | **высокий** | **закрыт (этап 16)** — `TokenService` читает `balanceOf` последовательно; пакетный вызов вынесен в A-59 |
| A-59 | Балансы токенов читаются по одному вызову на токен. При десятке отслеживаемых токенов это десять обращений к узлу на каждое обновление. Пакетный вызов через Multicall3 сократил бы их до одного, но требует доверия к отдельному контракту и проверки его наличия в сети | 16 | средний | открыт |
| A-60 | Балансы токенов не кэшируются: `BalanceService` кэширует только нативный. Каждое открытие раздела активов перезапрашивает все токены заново | 16 | средний | открыт |
| A-61 | `TokenService.detect` реализован, но из интерфейса недоступен. Он опирается на выборку журналов без указания контракта, которую публичные узлы отвергают (см. A-48), поэтому кнопка «найти токены» чаще всего показывала бы отказ | 16 | низкий | открыт |
| A-62 | Список токенов хранится по сетям, но при переключении сети перечитывается целиком из хранилища только при открытии сессии. Токен, добавленный в одной сети, виден в списке другой сети лишь после перезапуска | 16 | низкий | открыт |
| A-27 | `BalanceService.getAll` возвращает пустой список токенов | 12 | средний | **закрыт (этап 16)** |
| A-28 | Переименование и скрытие аккаунта из интерфейса. `AccountManager` их поддерживает, панель — нет | 12 | средний | открыт |
| A-29 | Добавление, изменение и удаление пользовательской сети из интерфейса | 12 | средний | **закрыт (этап 15)** — добавление и удаление; изменение вынесено в A-56 |
| A-54 | Проверка на подмену сравнивала имена побайтово и не ловила похожие символы разных алфавитов. Закрыто (этап 26): имена сравниваются по «скелету» — кириллица, греческий, армянский, цифры на месте букв, невидимые символы и математические начертания приводятся к латинице. Чужие буквы показываются пользователю | 15 | **высокий** | **закрыт (этап 26)** |
| A-55 | Форма добавления принимает один RPC-адрес. `INetworkConfig` хранит список: без резервных адресов пользовательская сеть теряет работоспособность при отказе единственного узла | 15 | средний | открыт |
| A-56 | Изменение пользовательской сети из интерфейса. `NetworkService.update` реализован с этапа 3, экрана нет: чтобы поправить опечатку в адресе, сеть приходится удалять и добавлять заново | 15 | средний | открыт |
| A-57 | Удаление сети не спрашивает подтверждения. Операция обратима — сеть добавляется заново, — но пользовательские RPC-адреса этой сети остаются в хранилище осиротевшими | 15 | средний | открыт |
| A-58 | Поддержка EIP-1559 у пользовательской сети выставляется в `false` без проверки. Узел умеет сообщить `baseFeePerGas`; консервативное предположение приведёт к переплате за газ в сети, которая EIP-1559 поддерживает | 15 | средний | открыт |
| A-64 | Отправка токенов ERC-20 и NFT не реализована: экран переводит только нативную валюту. При переводе токена поле `to` — адрес контракта, а получатель лежит в данных вызова, и сведение двух операций к одной форме нарушило бы главное свойство экрана — совпадение показанного с подписываемым. Нужен отдельный путь с собственным подтверждением | 17 | **высокий** | **закрыт (этап 26)** — отправляются и токены ERC-20, и предметы ERC-721/ERC-1155 |
| A-65 | Ускорение и отмена транзакции не реализованы: `prepareSpeedUp` и `prepareCancel` бросают `NotImplementedError`. Зависшая транзакция с низкой комиссией блокирует все последующие — nonce занят, и до её включения ни одна другая операция из этого аккаунта не пройдёт | 17 | **высокий** | **закрыт (этап 26)** — замена по тому же nonce с надбавкой 15 % к обеим частям комиссии; ускорение повторяет исходную операцию, отмена занимает номер переводом самому себе |
| A-66 | Статус отправленной транзакции не отслеживается: `startTracking` и `stopTracking` пусты, запись навсегда остаётся в состоянии «в очереди». Пользователь не узнает ни о включении в блок, ни об откате. Нужен опрос квитанции с ограничением частоты и остановкой по числу подтверждений | 17 | **высокий** | **закрыт (этап 26)** — опрос квитанций с учётом реорганизации цепи, глубины подтверждения и замещения по nonce |
| A-67 | Сеть и аккаунт на экране отправки не выбираются: они наследуются из настроек. Форма это оговаривает, но переключаться приходится через другой раздел. Нужны переключатели на самом экране со сбросом подготовленной транзакции — она содержит `chainId`, nonce и адрес отправителя | 17 | средний | открыт |
| A-68 | Кнопки «отправить всё» нет. Вычесть комиссию из суммы нельзя без итерации: изменение суммы меняет оценку газа. Нужен расчёт по верхней границе комиссии с явным показом остатка | 17 | средний | открыт |
| A-69 | Множители срочности (100 %, 125 %, 175 %) зашиты в `TransactionService` и не настраиваются. Экран ручного ввода `maxFeePerGas` и `maxPriorityFeePerGas` отсутствует; в перегруженной сети выбор из трёх значений может не покрыть нужную комиссию | 17 | средний | открыт |
| A-70 | Ожидаемое время включения не показывается: `estimatedSeconds` всегда `null`. Честная оценка требует статистики включения по уровням комиссии — данных, которых у кошелька нет. Назвать время наугад значило бы дать обещание, которое кошелёк не может выполнить | 17 | низкий | открыт |
| A-110 | Ускорение недоступно для отправок, сделанных до этапа 26: данных вызова и лимита газа в тех записях нет, восстановить их неоткуда, а догадка отправила бы под тем же номером другую операцию. Отмена для них работает. Со временем такие записи исчезнут сами | 26 | низкий | открыт |
| A-111 | Заменить зависшую отправку можно только из раздела «История». На главном экране те же записи показаны без кнопок: подготовка, подтверждение паролем и обработка отказа живут в `ActivityPage`, и повторять их во втором месте — прямой путь к расхождению поведения. Нужен общий контейнер шага замены | 26 | средний | открыт |
| A-112 | Надбавка при замене фиксирована (15 %) и не настраивается. В перегруженной сети замену приходится повторять несколько раз вместо одного шага с нужной комиссией. Смыкается с A-69 — экраном ручного ввода комиссии | 26 | низкий | открыт |
| A-113 | Отправка NFT (ERC-721 и ERC-1155) не реализована. Технически это `safeTransferFrom`, но отправлять нечего: кошелёк не читает принадлежащие адресу предметы, и форма свелась бы к ручному вводу адреса контракта и номера предмета. Сначала нужен список владения (A-114), потом отправка | 26 | **высокий** | **закрыт (этап 26)** — безопасная передача обоих стандартов с проверкой принадлежности до подписи |
| A-114 | Владение NFT не отслеживается: галереи нет, страница NFT — заглушка. Разбор журналов `Transfer` даёт кандидатов, но принадлежность на текущий момент требует проверки `ownerOf` либо `balanceOf` по каждому. Изображения отдельно: ссылки задаёт автор контракта, и загрузка раскроет IP-адрес владельца | 26 | средний | **закрыт (этап 26)** — поиск по журналам с проверкой принадлежности; изображения не грузятся намеренно |
| A-115 | Отправка токена не предупреждает, что получатель — адрес самого контракта токена. Токены, посланные на собственный контракт, теряются безвозвратно; сейчас показывается общее предупреждение «получатель — контракт» | 26 | средний | **закрыт (этап 28)** — отдельное предупреждение при отправке токена в его контракт; передача предмета в свою коллекцию отвергается до подтверждения |
| A-116 | Метаданные в истории подставляются только для отслеживаемых токенов. Перевод незнакомого токена показывается в необработанных единицах — честно, но нечитаемо. Чтение `decimals` и `symbol` по требованию раскрыло бы узлу интерес к контракту и требует кэша | 26 | низкий | открыт |
| A-117 | Проверка «позволяют выбрать срок автоблокировки» падает при полной нагрузке набора и проходит в одиночку: ожидание упирается в таймаут, когда параллельные потоки заняты выводом ключей. Тест зависит от скорости машины, а не от поведения приложения | 26 | средний | открыт |
| A-118 | Список NFT ограничен окном в 10 000 блоков и шестьюдесятью проверками принадлежности. Предмет, полученный раньше окна и с тех пор не двигавшийся, в список не попадёт. Полнота требует индексатора — то есть передачи адреса стороннему сервису | 26 | средний | открыт |
| A-119 | Изображения и описания предметов не показываются: ссылки на них задаёт автор контракта, и загрузка раскроет IP-адрес владельца произвольному серверу. Нужен либо прокси, либо явное согласие с предупреждением | 26 | средний | открыт |
| A-120 | Передача NFT не предупреждает, что получатель — контракт. `safeTransferFrom` отвергнет контракт, не умеющий принимать предметы, но принимающий контракт может оказаться не тем, чего хотел владелец | 26 | низкий | открыт |
| A-121 | Количество экземпляров ERC-1155 вводится строкой без проверки: нечисловое значение уходит в `BigInt` и даёт исключение вместо понятного сообщения | 26 | низкий | открыт |
| A-122 | `WalletSession` — 1565 строк, 34 публичных метода, 27 приватных полей; `TransactionService` — 1001 строка и четыре пути подготовки плюс слежение. Обязанность у каждого пока одна, но следующая возможность сделает их God Object. Нужны фасады по областям данных и вынос слежения в `TransactionTracker` | 26 (аудит) | средний | **отложен сознательно (этап 28)** — чистый рефакторинг центрального класса без новых возможностей: риск сломать работающее выше пользы. Возвращаться при следующей крупной возможности, которая добавит сессии ещё один путь данных |
| A-123 | Кодировка ABI продублирована: `WORD_LENGTH`, `ADDRESS_LENGTH`, `MAX_UINT256` и чтение адреса из слова объявлены и в `core/token/erc20.ts`, и в `core/nft/abi.ts`. Расхождение копий проверки адреса даст неверного получателя на экране подтверждения. Нужен общий `core/abi` | 26 (аудит) | средний | **закрыт (этап 28)** — примитивы кодировки вынесены в `core/abi`; четыре копии сведены в одну, включая три разных проверки выравнивания адреса |
| A-124 | Чтение истории транзакций линейно: `#readAll` расшифровывает все записи на каждый вызов. Замер: 100 записей — 8.4 мс, 500 — 69.6 мс. Слежение вызывает `findUnsettled` каждые 12 секунд, то есть цена платится постоянно и растёт со временем пользования. Нужен индекс «адрес + сеть → хэши» либо индексы IndexedDB | 26 (аудит) | **высокий** | **закрыт (этап 28)** — индекс «адрес плюс сеть» и список незавершённых; слежение на 500 записях: 69.6 → 1.2 мс |
| A-125 | Сетевой слой без дедупликации запросов в полёте и без JSON-RPC batch: список токенов — по вызову на токен, поиск предметов — до 60 вызовов, параллельные экраны запрашивают один баланс дважды. Публичные узлы ограничивают частоту, отказ вероятен | 26 (аудит) | средний | открыт |
| A-126 | Реестр долга расходился с действительностью: A-41 и A-63 числились открытыми после выполнения работ. Расхождение обесценивает реестр быстрее, чем отсутствие записей; нужна сверка при закрытии каждого этапа | 26 (аудит) | низкий | **закрыт (этап 26)** — записи приведены в соответствие |
| A-127 | `NftService.clear()` реализован, но не вызывается в `WalletSession.close()`, хотя `EnsService.clearCache()` вызывается специально. Кэш названий коллекций переживает блокировку — не секрет, но нарушение правила «сессия уносит с собой всё, что накопила» | 26 (аудит) | низкий | **закрыт (этап 27)** — кэши NFT и разрешений очищаются при закрытии сессии |
| A-128 | `ErrorBoundary` отсутствует: любая ошибка рендера обрушивает интерфейс в белый экран. Для владельца средств это неотличимо от пропажи. Нужен экран отказа, прямо говорящий, что средства на месте, и дающий ссылку на обозреватель | 26 (аудит) | **высокий** | **закрыт (этап 27)** — экран отказа прямо сообщает, что средства целы, и называет причину |
| A-129 | Поведение в двух вкладках не проверялось: обе работают с одним хранилищем IndexedDB, и состояние сессий может разойтись — например, при отправке из одной вкладки при открытой второй | 26 (аудит) | средний | **закрыт (этап 28)** — проверено шестью сквозными сценариями; найдена и устранена вкладка-зомби после стирания кошелька |
| A-130 | Кошельки, созданные до замены почты именем, читают подпись из прежнего ключа `user.email` при каждом чтении. Значение не переписывается: миграция при чтении — источник неожиданных записей в хранилище. Ключ можно убрать, когда таких кошельков заведомо не останется | 27 | низкий | открыт |
| A-131 | Язык интерфейса один, и словарь используется лишь на нескольких экранах: остальные тексты записаны прямо в разметке. Пока язык один, это не мешает; добавление второго потребует вынести в словарь около семисот строк | 27 | низкий | открыт |
| A-132 | Комментарии, названия проверок и документация остаются на русском. Для одного разработчика это дешевле, но участие англоязычного соавтора потребует перевода примерно двадцати тысяч строк комментариев | 27 | низкий | открыт |
| A-133 | Список проверенных контрактов обновляется только вместе со сборкой. Контракт с обновляемой реализацией может изменить символ — так уже случилось с мостом Tether, — и запись устареет молча. Расхождение кошелёк показывает, но узнаёт о нём лишь при добавлении токена | 28 | средний | открыт |
| A-134 | В списке только стейблкоины и обёрнутые валюты сетей: двадцать два контракта. Токены за его пределами помечаются непроверенными, хотя большинство из них законны. Расширять список можно только сверенными адресами — «по спискам из интернета» значит поручиться за то, чего никто не проверял | 28 | низкий | открыт |
| A-135 | История одного адреса по-прежнему читает все его записи: при пятистах это 35 мс. Дальше нужна постраничная выдача (смыкается с A-71), а не индекс — записи действительно принадлежат этому адресу | 28 | низкий | открыт |
| A-142 | Проверка вызова — это `eth_call`, а не трассировка: изменения балансов она не показывает. Полная симуляция требует `debug_traceCall` либо подмены состояния, чего публичные узлы не дают | 26 | средний | открыт |
| A-143 | Отказ значением распознаётся у трёх вызовов ERC-20 (`transfer`, `transferFrom`, `approve`). Прочие функции с булевым результатом молчаливый отказ не выявят | 26 | низкий | открыт |
| A-144 | Собственные ошибки контрактов показываются четырёхбайтовым признаком: расшифровать их без описания контракта нельзя. Каталог известных признаков помог бы, но это ещё один внешний источник | 26 | низкий | открыт |
| A-145 | Прогон добавляет один вызов к узлу на каждую подготовку транзакции: оператор узнаёт о намерении отправить до самой отправки. Отменить проверку пользователь не может | 26 | средний | открыт |
| A-136 | Две вкладки берут nonce независимо: одновременная отправка из обеих даст один номер, и вторая транзакция заменит первую вместо очереди. Проверить это в тестовой среде нечем — узлов нет; нужен либо общий счётчик через канал вкладок, либо предупреждение при второй открытой вкладке | 28 | средний | открыт |
| A-137 | Изменения, сделанные в одной вкладке (добавленный токен, своя сеть, RPC-адрес), вторая видит только после перезагрузки. Канал вкладок для этого уже есть — не хватает событий и их обработки | 28 | низкий | открыт |
| A-138 | Выбор активной сети по-прежнему лежит открытым в настройках: он нужен до полной загрузки и секретом не является, но раскрывает, какими сетями пользуется владелец. Перенос требует решения о том, что делать до разблокировки | 28 | низкий | открыт |
| A-139 | Поиск занятых адресов не видит те, где лежат только токены либо предметы: у них нулевой баланс и нулевой счётчик. Чтобы найти их, пришлось бы опрашивать каждый контракт по каждому адресу — десятки запросов вместо двух. Ограничение названо в интерфейсе | 28 | средний | открыт |
| A-140 | Поиск идёт только по активной сети. Аккаунт, которым пользовались лишь в другой сети, не найдётся, пока владелец туда не переключится и не повторит поиск | 28 | низкий | открыт |
| A-141 | В Polygon, Arbitrum и BNB Chain выборка журналов на публичных узлах не работает: бесплатный доступ её не отдаёт либо узел отвечает отказом. История в этих сетях требует своего узла или ключа индексатора; кошелёк говорит об этом прямо, но сам сделать ничего не может | 28 | средний | открыт |
| A-142 | Пределы публичных узлов меняются без предупреждения, а список в коде обновляется только со сборкой. Проверка доступности узлов есть в настройках, но она ручная: автоматической сверки при отказе истории нет | 28 | низкий | открыт |
| A-30 | Ссылка на обозреватель блоков для операции. `blockExplorerUrls` есть в конфигурации сети, в списке операций не используется | 12 | низкий | открыт |
| A-31 | QR-код адреса на экране получения. Требует библиотеки генерации; ручной ввод адреса — источник ошибок | 12 | низкий | открыт |
| A-32 | Сводная стоимость портфеля. Требует источника курсов, то есть ещё одного стороннего сервиса, видящего адреса пользователя | 12 | низкий | открыт |
| A-22 | Экран восстановления принимает только 12 и 24 слова. BIP-39 допускает также 15, 18 и 21; `MnemonicService` их поддерживает, интерфейс — нет | 11 | низкий | открыт |
| A-23 | Автодополнение слов по префиксу при вводе seed-фразы. `findWordsByPrefix` реализован в контракте, интерфейс им не пользуется | 11 | низкий | открыт |
| A-24 | Выбор языка словаря BIP-39 при восстановлении. Сейчас подразумевается английский | 11 | низкий | открыт |

## Временные послабления — вернуть по указанию

Включаются одной константой `IS_TEST_MODE` в `src/shared/config/test-mode.ts`.
Строки этого раздела закрываются возвратом её в `false`.

| № | Задача | Этап | Приоритет | Статус |
| --- | --- | --- | --- | --- |
| T-1 | Проверка записанной seed-фразы была снята при создании кошелька | 19 | **высокий** | **возвращена (этап 21)** — `IS_TEST_MODE = false` |
| T-2 | Вход по seed-фразе был скрыт вместе с маршрутом | 19 | **высокий** | **возвращён (этап 21)** — `IS_TEST_MODE = false` |
| T-3 | Боевая сборка с включённым флагом останавливается проверкой в точке входа. Проверку следует продублировать в CI, чтобы отказ наступал до развёртывания, а не при первом открытии | 19 | средний | открыт |

## Учётные данные и локализация

| № | Задача | Этап | Приоритет | Статус |
| --- | --- | --- | --- | --- |
| A-81 | Адрес почты сверяется после расшифровки хранилища и защиты от подбора не добавляет. Чтобы он что-то значил, его пришлось бы хранить открытым — то есть раскрыть личность владельца при доступе к диску | 19 | средний | **закрыт (этап 27)** — почта заменена именем пользователя, а сверка при входе убрана: она создавала впечатление второго фактора, которого нет |
| A-82 | Имя пользователя нельзя изменить после создания: экрана нет, метод отсутствует. Опечатка исправляется только пересозданием кошелька. Смыкается с переименованием аккаунтов (A-28) | 19 | средний | открыт |
| A-83 | Имя из адреса получает только первый аккаунт; второй и последующие нумеруются как раньше. Переименование аккаунтов из интерфейса не реализовано (см. A-28) | 19 | низкий | **частично закрыт (этап 27)** — имя пользователя подписывает первый аккаунт и показывается в настройках; переименование по-прежнему недоступно |
| A-84 | Кнопка «Смарт контракт» кликабельна и не делает ничего — решение заказчика. Кнопка без обратной связи читается пользователем как поломка интерфейса; при появлении раздела заменить переходом | 19 | средний | открыт |
| A-85 | Переведены только экраны входа, панель и навигация. Отправка, история, портфель, активы, NFT и настройки остаются на русском при выбранном английском — смешанный язык хуже одного неродного | 19 | **высокий** | **закрыт (этап 27)** — интерфейс целиком на английском, второго языка нет |
| A-86 | Выбор языка не сохраняется между сессиями: живёт в состоянии React. Ключ `SETTINGS_KEY.Language` выделен, постоянное хранилище появится вместе с A-20 | 19 | средний | открыт |
| A-87 | Сообщения об ошибках ядра (`InvalidPasswordError` и прочие) приходят на русском независимо от выбранного языка: тексты зашиты в классы ошибок. Нужны коды ошибок и перевод на стороне интерфейса | 19 | средний | открыт |
| A-88 | Форматирование дат и чисел привязано к `ru-RU` в нескольких местах и не следует за выбранным языком | 19 | низкий | открыт |
| S-44 | Адрес почты попадает в журнал как имя аккаунта, минуя проверку по имени поля | 19 | средний | **закрыт (этап 19)** — `ConsoleLogger` скрывает адрес и по имени поля, и по виду значения |

## Подключение приложений

| № | Задача | Этап | Приоритет | Статус |
| --- | --- | --- | --- | --- |
| A-90 | Подключение начиналось только вставкой ссылки вручную. Чтение штрих-кода добавлено (этап 26): камера по требованию, разбор на устройстве, кадры никуда не уходят; посторонний код называется посторонним. Без камеры и при отказе в доступе остаётся вставка ссылки | 21 | **высокий** | **закрыт (этап 26)** |
| A-91 | Запись устарела: установленная версия библиотеки заводит собственную базу IndexedDB, и сессии перезагрузку переживают. Настоящая беда была в другом — записи лежали открытым текстом, вне нашего учёта и переживали удаление кошелька. Закрыто (этап 26): состояние подключений хранится в зашифрованном пространстве кошелька | 21 | **высокий** | **закрыт (этап 26)** |
| A-92 | Смена активной сети и аккаунта не сообщается подключённым приложениям (`chainChanged`, `accountsChanged`): приложение продолжит считать активным прежнее | 21 | средний | открыт |
| A-93 | Запросы обрабатываются по одному, второй отклоняется с причиной «кошелёк занят». Приложение вправе повторить, но очередь была бы удобнее — при условии, что экраны не накладываются друг на друга | 21 | низкий | открыт |
| A-94 | Метод `wallet_switchEthereumChain` не поддержан: приложение не может попросить кошелёк сменить сеть, а сверка сети выдаётся замечанием | 21 | средний | открыт |
| A-95 | Разбор данных вызова ограничен `approve` и `setApprovalForAll`. Остальные вызовы помечаются как нераспознанные — честно, но неинформативно. Полноценный декодер ABI требует источника описаний контрактов | 21 | средний | открыт |
| A-146 | Чтение кадров идёт в основном потоке четыре раза в секунду. На слабом телефоне это заметно; перенос разбора в worker убрал бы задержку интерфейса | 26 | низкий | открыт |
| A-147 | Выбрать камеру нельзя: запрашивается задняя, и на устройстве с несколькими объективами берётся любая из них | 26 | низкий | открыт |
| A-148 | **Аппаратный кошелёк не проверен на живом устройстве.** Протокол и обмен покрыты тестами против дублёра, но ошибка в понимании самого протокола так не обнаруживается. До проверки на настоящем Ledger способ считать рабочим нельзя | 26 | **высокий** | открыт |
| A-149 | Поддержан только Ledger по WebHID. Trezor требует внешнего окна на домене производителя — это сторонний код рядом с кошельком, и решение о нём отдельное | 26 | средний | открыт |
| A-150 | Разбиение данных подписи идёт по 255 байт без учёта границ полей: для унаследованных транзакций старые прошивки требуют, чтобы `chainId` не разрывался между частями. Проверяется только на устройстве | 26 | средний | открыт |
| A-151 | Структура EIP-712 уходит на устройство двумя хэшами: на его экране видны не поля, а хэши. Разбор структуры прошивкой поддержан не везде, и попытка кончалась бы отказом вместо подписи | 26 | средний | открыт |
| A-152 | Соединение открывается заново на каждую операцию: чтение пяти адресов — пять запросов разрешения браузера, если оно не выдано постоянно | 26 | низкий | открыт |
| A-153 | WebHID существует только в браузерах на Chromium. В Firefox и Safari раздел показывает отказ и предлагает вставку ссылки — иного пути нет | 26 | низкий | открыт |
| A-154 | Версия схемы хранилища выводится из числа миграций, а список хранилищ — из перечня областей. Расхождение теперь лечится само (переоткрытием), но связать эти две величины напрямую было бы надёжнее | 26 | средний | открыт |
| A-155 | Ограничения на частоту проверок копии нет: пароль спрашивается каждый раз, но задержки между попытками, как у разблокировки, здесь не применяются | 26 | низкий | открыт |
| A-156 | Введённая для проверки фраза живёт строкой в состоянии React и затиранию не поддаётся — как и при импорте. Это свойство строк JavaScript, обходится только полем ввода на буферах | 26 | низкий | открыт |
| S-52 | Заголовки размещения порождаются сборкой, но проверить их применение можно только на живом хостинге: сквозные проверки идут против `vite preview`, который файл `_headers` не читает | 26 | средний | открыт |
| S-53 | Отчётов о нарушениях политики нет (`report-to`): владелец размещения не узнает о попытках обойти её. Требует адреса приёмника, то есть ещё одного стороннего сервиса | 26 | низкий | открыт |
| A-157 | Готовы файлы для Netlify, Cloudflare Pages и nginx. Для Vercel, Caddy и Apache настройку придётся переносить руками | 26 | низкий | открыт |
| S-54 | Собственная база библиотеки (`WALLET_CONNECT_V2_INDEXED_DB`) у прежних установок остаётся на диске с ключами сессий открытым текстом. Удалять её по имени изнутри кошелька — значит зашить внутреннее имя чужого пакета: сменится при обновлении, и удаление молча перестанет работать, создавая ложную уверенность | 26 | средний | открыт |
| A-158 | При автоблокировке во время работы с подключениями запись состояния отказывает: библиотека получает исключение посреди обмена. Нужна либо приостановка транспорта при блокировке, либо отсрочка блокировки на время запроса | 26 | средний | открыт |
| A-159 | Замена хранилища проверена тестами против настоящего зашифрованного хранилища, но не живым подключением: для этого нужен идентификатор проекта WalletConnect и стороннее приложение | 26 | средний | открыт |
| S-55 | Взято подмножество смешиваемых символов, а не полная таблица UTS #39: она содержит тысячи записей и весит больше всего сетевого слоя. Подмена буквой из письменности вне перечня (эфиопской, чероки) не распознаётся | 26 | средний | открыт |
| S-56 | Многосимвольные подмены не ловятся: `rn` вместо `m` в мелком шрифте неотличимо. Односимвольная таблица их не покрывает, а добавление пар даёт ложные срабатывания на обычных словах | 26 | низкий | открыт |
| S-58 | Эталон для токенов — список из 22 проверенных контрактов. Подделка под токен вне списка сравнивается не с чем и не распознаётся | 26 | средний | открыт |
| S-59 | Смешение письменностей распознаётся среди латиницы, кириллицы, греческого и армянского. Имя целиком в одной чужой письменности смешением не является и не помечается — при этом выглядеть оно может как латинское | 26 | низкий | открыт |
| S-60 | Подделка под токен проверяется при добавлении вручную. Токены, появляющиеся в списке иначе (из истории переводов), той же проверки не проходят | 26 | средний | открыт |
| S-57 | По «скелету» сравнивались только имена сетей. Закрыто (этап 26): символ и имя токена сверяются с проверенным списком своей сети, добавление подделки требует явного согласия; у имён приложений эталона нет, и там показывается смешение письменностей внутри слова | 26 | средний | **закрыт (этап 26)** |
| A-96 | Основной чанк вырос до 842 kB (gzip 279 kB). WalletConnect вынесен отдельно и в него не входит, но сам основной по-прежнему тянет весь сетевой слой на экраны входа. Уточняет P-14 | 21 | **высокий** | **закрыт (этап 24)** — начальный набор 529 kB (190.61 kB gzip); сетевой слой на экраны входа больше не попадает |

## Резервная копия

| № | Задача | Этап | Приоритет | Статус |
| --- | --- | --- | --- | --- |
| A-97 | Проверки записанной копии не было. Добавлена (этап 26): владелец вводит переписанное и получает ответ «совпадает/нет», фраза не показывается ни при каком исходе. Требуется пароль — иначе экран становится оракулом для перебора чужих догадок; сравнение без досрочного выхода | 22 | **высокий** | **закрыт (этап 26)** |
| A-98 | Приватный ключ выдаётся только для активного аккаунта: выбрать другой прямо на экране копии нельзя, нужно возвращаться в настройки и переключаться | 22 | средний | открыт |
| A-99 | Экспорт расширенных ключей (`xpub`, `xprv`) интерфейсом не выведен, хотя `ExportGuard` и `HDWalletService` их поддерживают. Пока не выведен `xpub`, оценка `AccountCompromise` на практике недостижима — она проверяется только тестами | 22 | средний | открыт |
| A-100 | Журнал экспортов пользователю не показывается. `ExportGuard.getHistory` реализован, экрана нет: владелец не может увидеть, что и когда выгружалось с его устройства | 22 | средний | открыт |
| A-101 | Экран резервной копии не переведён на английский — как и остальные экраны кошелька. Часть общего долга A-85 | 22 | средний | **закрыт (этап 27)** — экран переведён вместе с остальными |
| A-102 | Импорт фразы длиной 15, 18 и 21 слова принимается ядром, но подпись поля и текст описания называют только 12 и 24. Уточняет A-22 | 22 | низкий | открыт |
| S-55 | Пароль при выдаче приватного ключа выводится дважды (600 000 итераций PBKDF2 каждый раз): один вывод в `BackupManager` ради корректного порядка записи в журнал, второй внутри `AccountManager`. Устранимо только передачей результата проверки как значения — а значит, введением объекта «пароль подтверждён», который придётся защищать от подделки | 22 | низкий | открыт |
| S-56 | Форма пароля на экране копии не ограничивает число попыток — тот же недостаток, что у S-47 и S-12. Подбор через неё идёт с той же скоростью, что и через экран разблокировки | 22 | средний | открыт |
| S-57 | Выданная фраза остаётся в дереве React строками до ухода с экрана и в куче — до сборки мусора. Ограничение среды, устранить нельзя; сокращено тем, что буфер затирается сразу после разбора на слова, а строки не поднимаются выше страницы | 22 | низкий | принят как ограничение |

## Имена ENS

| № | Задача | Этап | Приоритет | Статус |
| --- | --- | --- | --- | --- |
| A-103 | Имена с эмодзи и национальными алфавитами не поддерживаются: вместо нормализации ENSIP-15 принимается набор «латиница, цифры, дефис» | 23 | средний | **закрыт (этап 23)** — подключена `@adraffy/ens-normalize`, эталонная реализация ENSIP-15. Измеренная цена — 0.17 kB gzip: библиотека уже входила в граф через ethers |
| S-61 | ENSIP-15 запрещает смешение письменностей внутри метки, но имя, целиком записанное другой письменностью и похожее по начертанию на латинское, остаётся законным и принадлежит другому лицу. Запретить такие имена нельзя. Смягчение — оговорка «имя записано не латиницей» рядом с адресом; полноценного решения не существует ни у одного кошелька | 23 | средний | смягчён |
| S-62 | **Дефект.** Запрос приложения на развёртывание контракта (`to` отсутствует) исполняется как перевод самому себе: `WalletSession.executeDappRequest` подставляет `to: payload.transaction.to ?? payload.transaction.from`. Пользователю показано «создаёт контракт», подписывается другое; газ списывается, одобренная операция не выполняется. Передавать `to: null` либо отвергать запрос | 26 (аудит) | **высокий** | **закрыт (этап 27)** — получатель передаётся как прислан, оценка газа уходит узлу без поля `to` |
| S-63 | Метод `eth_sign` принимается наравне с `personal_sign`. Пролома нет — подпись идёт через `hashMessage` с префиксом EIP-191, поэтому подменить транзакцию нельзя, — но это несоответствие спецификации, работающее в безопасную сторону по совпадению. MetaMask метод отключил, затем удалил; следует отвергать на входе | 26 (аудит) | средний | **закрыт (этап 27)** — метод отвергается на входе и не заявляется приложению при подключении |
| S-64 | Пользовательские RPC-адреса хранятся открытым текстом вместе с конфигурациями сетей, а такой адрес может содержать ключ доступа прямо в URL. Встроенные адреса могут остаться открытыми, пользовательские — нет | 26 (аудит) | средний | **закрыт (этап 28)** — конфигурации сетей шифруются, прежние записи переносятся при первом чтении; адреса из `CustomRpcProvider` шифровались и раньше |
| A-104 | ENS работает только при активной сети Ethereum. Разрешение имён из L2 требует второго соединения с узлом Ethereum — то есть согласия владельца на ещё одного оператора, видящего его запросы. Нужен явный переключатель, а не умолчание | 23 | **высокий** | открыт |
| A-105 | Оффчейн-резолверы (EIP-3668, CCIP-Read) и подстановочное разрешение ENSIP-10 не поддержаны: имена вида `имя.cb.id` и большинство имён Base не разрешаются. Поддержка означает HTTP-запрос по адресу, названному контрактом, — отдельное решение о доверии шлюзам | 23 | средний | открыт |
| A-106 | Контрагенты в истории переводов показываются адресами: обратное разрешение каждой строки — два обращения к узлу на запись и подробный след у оператора. Нужен ограниченный по числу и кэшируемый вариант | 23 | средний | открыт |
| A-107 | Аватар ENS (`text(node, 'avatar')`) не читается: аккаунты по-прежнему рисуются отпечатком адреса | 23 | низкий | открыт |
| A-108 | Отправить средства по имени можно, а увидеть своё имя при получении — нет: экран получения показывает только адрес | 23 | низкий | открыт |
| A-109 | Кэш имён живёт пять минут в памяти сессии и теряется при блокировке. Для подписи собственных аккаунтов это означает повторный запрос при каждой разблокировке | 23 | низкий | открыт |
| S-58 | Разрешение имени сообщает оператору узла, кому пользователь собирается платить, — сверх того, что узел видит при отправке. Смягчения нет: запрос узлу необходим. Собственный узел эту утечку закрывает | 23 | средний | открыт |
| S-59 | Имя может сменить владельца между показом и отправкой: адрес разрешается один раз, при вводе. Экран подтверждения предупреждает об этом текстом, но повторной сверки перед публикацией нет | 23 | средний | открыт |
| S-60 | Истёкшая регистрация не отличается от действующей: реестр отвечает адресом, пока запись не переписана. Проверка срока требует обращения к контракту регистратора | 23 | низкий | открыт |

## Справочный сервис

| № | Задача | Этап | Приоритет | Статус |
| --- | --- | --- | --- | --- |
| B-1 | Расширение с сервисом не связано: клиент каталоги не запрашивает, уведомления не показывает, версию не проверяет, настройки не синхронизирует. Сервис существует и проверен, но пользователю пока ничего не даёт. Подключение — отдельный этап, включая шифрование настроек на устройстве и порождение идентификатора синхронизации | 17 | **высокий** | открыт |
| B-2 | Контракт API описан в `server/src/api/contracts.ts` и клиенту недоступен. При подключении типы придётся либо продублировать, либо вынести в общий пакет. Дублирование разойдётся с реализацией при первом же изменении | 17 | **высокий** | открыт |
| B-3 | Каталог отдаётся без подписи. Скомпрометированный сервис может подменить адрес рекомендуемого токена, и клиент этого не заметит. Нужна подпись каталога ключом выпуска и проверка на клиенте — тогда компрометация сервера перестаёт быть достаточной | 17 | **высокий** | открыт |
| B-4 | Хранилище настроек живёт в памяти процесса: перезапуск стирает записи. Допустимо, пока синхронизация — зеркало локальных настроек, но постоянное хранилище нужно до боевого использования. Интерфейс `ISettingsRepository` для замены выделен | 17 | **высокий** | открыт |
| B-5 | Ограничение частоты общее для всех маршрутов и хранится в памяти процесса: при нескольких экземплярах сервиса предел умножается на их число. Нужен общий счётчик и отдельный, более жёсткий предел для записи настроек — у неё есть стоимость | 17 | средний | открыт |
| B-6 | Каталог не имеет `ETag`: клиент перезапрашивает его целиком по истечении срока кэша. Каждое обращение раскрывает оператору сервиса факт работы пользователя | 17 | средний | открыт |
| B-7 | Токены сверены только для пяти сетей из семи: BNB Chain и Avalanche не покрыты использованным списком. Нужен второй независимый источник для них — сверка по одному источнику не является сверкой | 17 | средний | открыт |
| B-8 | Сверка каталога с контрактами выполнена однократно и вручную. Адрес токена может смениться (миграция контракта), и каталог устареет молча. Нужен регулярный автоматический прогон сверки, останавливающий выпуск при расхождении | 17 | средний | открыт |
| B-9 | Признак поддержки EIP-1559 в каталоге сетей заявлен, а не измерен: узел умеет сообщить `baseFeePerGas`. Совпадает с A-58 на стороне клиента | 17 | низкий | открыт |
| B-10 | Наблюдения за сервисом нет: ни метрик, ни отдельного счётчика срабатываний охранника входящих данных. Попытка отправить ключевой материал — событие, которое обязано быть видно, а не только записано строкой в журнале | 17 | средний | открыт |
| B-11 | Развёртывания нет: ни контейнера, ни описания окружения, ни проверки готовности отдельно от проверки живости | 17 | средний | открыт |
| B-12 | Признак секрета для мнемонической фразы опирается на образец «двенадцать коротких слов подряд», а не на словарь BIP-39. Фраза из более длинных слов признак не сработает. Полный словарь весит сотни килобайт и потребует решения, стоит ли того | 17 | низкий | открыт |

## Инфраструктура

| № | Задача | Этап | Приоритет | Статус |
| --- | --- | --- | --- | --- |
| I-1 | Инициализировать git-репозиторий | 1 | **высокий** | **закрыт (этап 26)** — репозиторий создан, первый коммит охватывает 441 файл, `.gitattributes` закрепляет LF |
| I-2 | Хуки pre-commit: `husky` + `lint-staged`, прогон `verify` по изменённым файлам | 1 | средний | открыт |
| I-3 | CI на `npm run verify` и `npm audit --audit-level=high` | 1 | средний | открыт |
| I-4 | `npm ci` с закреплённым lock-файлом, проверка provenance пакетов | 1 | средний | открыт |
| I-5 | Порог покрытия тестами в `vitest.coverage.thresholds` | 1 | низкий | открыт |
| I-6 | Миграция на TypeScript 7 после появления поддержки в `typescript-eslint` | 1 | низкий | открыт |
| I-7 | Storybook для UI-кита. Окупается после ~10 компонентов | 1 | низкий | открыт |

## Проверки

| № | Задача | Этап | Приоритет | Статус |
| --- | --- | --- | --- | --- |
| T-1 | Порога покрытия в настройках нет: `vitest.coverage.thresholds` не задан, и снижение покрытия ничего не ломает. Уточняет I-5 | 25 | средний | открыт |
| T-2 | Транспорт WalletConnect (`WalletConnectTransport`) покрыт на 6%: проверка требует живого relay либо подмены самой библиотеки. Решения о том, что именно подменять, пока нет | 25 | средний | открыт |
| T-3 | `CoinGeckoPriceProvider` покрыт частично: разбор ответов проверен, поведение при отказе и превышении частоты — нет | 25 | средний | открыт |
| T-4 | Сквозные проверки идут только в Chromium. Firefox и WebKit по-разному применяют Trusted Types, а именно на них держится защита от внедрения | 25 | средний | открыт |
| T-5 | Сквозные проверки не запускаются в CI: нужен шаг с установкой браузера и кэшированием ~115 МБ | 25 | средний | открыт |
| T-6 | `frame-ancestors` в meta-теге не поддерживается, поэтому защита от кликджекинга не проверяется вовсе — она обязана прийти заголовком от хостинга. Уточняет S-4 | 25 | средний | открыт |
| T-7 | Экраны создания кошелька и активов покрыты на 54% и 36%: не проверены шаги подтверждения записи фразы и форма импорта токена | 25 | низкий | открыт |
| T-8 | Проверки скорости работы отсутствуют: виртуализация, ограничение параллельности и кэш деривации проверены на поведение, но не на то, что они действительно ускоряют | 25 | низкий | открыт |

## Производительность

| № | Задача | Этап | Приоритет | Статус |
| --- | --- | --- | --- | --- |
| P-1 | Кэш выведенных адресов. Сейчас `getAddress(n)` каждый раз выполняет деривацию и операцию на кривой | 5 | средний | **закрыт (этап 24)** — кэшируется только публичная проекция (адрес, публичный ключ, путь); узлы `HDKey` с приватным ключом в кэш не попадают, очистка в `wipe()` |
| P-2 | Ограничение частоты `validate` при вводе seed-фразы. Сейчас проверка на каждое нажатие клавиши | 4 | низкий | открыт |
| P-3 | Кэш результата проверки chainId. При повторном добавлении сети узел опрашивается заново | 3 | низкий | открыт |
| P-4 | Ленивая подгрузка словарей BIP-39 — многоязычный импорт без веса в основном бандле | 4 | низкий | открыт |
| P-5 | Фоновая проверка доступности RPC-узлов при `init()`. Сейчас неработающий узел обнаружится только при первом запросе | 3 | низкий | открыт |
| P-6 | Вывод ключа PBKDF2 блокирует главный поток на сотни миллисекунд. Перенести разблокировку в Web Worker | 7 | средний | открыт |
| P-7 | Шестнадцатеричная запись двоичных полей контейнера удваивает объём. Заменить на base64 при появлении крупных зашифрованных значений | 7 | низкий | открыт |
| P-8 | Кэш результатов чтения (баланс, nonce) с коротким временем жизни. Сейчас каждый вызов уходит к узлу | 8 | средний | открыт |
| P-9 | Автоматический переход на резервный узел при отказе уже установленного соединения. Сейчас перебор выполняется только при создании | 8 | средний | **закрыт (этап 12.1)** |
| P-10 | Разблокировка не показывает индикатор ожидания: вывод ключа PBKDF2 занимает сотни миллисекунд, и кнопка выглядит зависшей. Связано с P-6 | 11 | средний | открыт |
| P-11 | `TransactionRepository.findByAddress` читает и расшифровывает все записи истории, чтобы отобрать нужные. При сотнях операций это заметно; нужен индекс по паре «адрес + сеть» | 12 | средний | открыт |
| P-12 | Кэш балансов не сохраняется между сессиями: после разблокировки экран ждёт ответа узла. Пространство имён `BalanceCache` для этого уже выделено | 12 | низкий | открыт |
| P-13 | Опрос баланса продолжается на скрытой вкладке. Нужна остановка по `visibilitychange`: фоновые запросы тратят лимиты узла и раскрывают активность | 12 | средний | **закрыт (этап 24)** — `setBackgroundRefreshEnabled` в сессии, слежение за видимостью в `WalletProvider`; возврат на вкладку обновляет баланс сразу |
| P-14 | Бандл вырос с 349 kB до 692 kB (gzip 239 kB): ethers попал в граф зависимостей приложения вместе с сетевым слоем. Экраны онбординга сетевого кода не требуют — нужен раздельный чанк с загрузкой при разблокировке | 12 | **высокий** | **закрыт (этап 24)** — ethers и разбор EIP-712 (220 kB, 69 kB gzip) грузятся при разблокировке; начальная загрузка 286.68 → 190.61 kB gzip |
| P-15 | Проверка доступности подключается к каждому узлу и сразу разрывает соединение. Для диагностики это верно, но действующее соединение при этом создаётся заново. Переиспользовать уже открытое | 12.1 | низкий | открыт |
| P-17 | Начальный чанк — 526 kB (188.75 kB gzip). В нём React, `@noble`/`@scure` и всё ядро, кроме подписи и транспорта. Дальнейшее сокращение требует отложенной сборки сессии кошелька: она меняет момент создания сервисов, то есть жизненный цикл блокировки | 24 | средний | открыт |
| P-18 | Виртуализация применена только к истории переводов. Списки токенов, NFT, аккаунтов и узлов по-прежнему рисуются целиком — они короче, но предела на них нет | 24 | низкий | открыт |
| P-19 | Высота строки истории задана числом и в разметке, и в коде виртуализации. Расхождение сдвигает список при прокрутке; проверки на совпадение нет | 24 | низкий | открыт |
| P-20 | Балансы токенов читаются по одному вызову на токен. Multicall свёл бы их в один запрос, но требует доверия к отдельному контракту и его адресу в каждой сети | 24 | средний | открыт |
| P-16 | Экран настроек рисует списки аккаунтов, сетей и узлов сразу. При десятках аккаунтов и семи сетях это заметно; нужны раскрывающиеся разделы либо виртуализация | 13 | низкий | открыт |

---

## Отклонено с обоснованием

| Задача | Причина отказа |
| --- | --- |
| DI-контейнер (InversifyJS, tsyringe) | Требует `emitDecoratorMetadata` — запрещено настройкой `erasableSyntaxOnly`; тянет `reflect-metadata` в бандл рядом с ключами; переносит проверку зависимостей из компиляции в рантайм |
| Внедряемый источник случайности | Возможность подменить генератор случайных чисел означает возможность сделать все ключи предсказуемыми. Тестируемость такой цены не стоит |
| Полностью закалённый путь деривации `m/44'/60'/0'/0'/n'` | Устранил бы риск «xpub + приватный ключ», но seed-фраза перестала бы восстанавливаться в MetaMask, Trust и Ledger. Запереть пользователя в одном кошельке хуже условного риска |
| `@noble/secp256k1` отдельно от `@noble/curves` | Две независимые реализации secp256k1 в одном бандле: лишний вес и два пути исполнения для самого критичного примитива |
| Словари BIP-39 всех языков | ~55 КБ gzip ради сценария, который почти не встречается. MetaMask тоже только английский |
