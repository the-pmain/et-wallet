---
description: One-branch workflow for isolated Safe and ETX clients
alwaysApply: true
---

# Multi-client workflow

- Daily work stays on `elm-wallet-main`. Push to remote `et-wallet`, not `origin`, unless James asks.
- Both clients live on this branch: `clients/safe` and `clients/etx`.
- Keep the client trees isolated. Never import one client from another or replace one UI with the other.
- `THEME=safe` or `THEME=etx` selects exactly one client at Vite startup/build time.
- Preserve storage identities: Safe uses `elmsafe`; ETX uses `etwallet`.
- Shared behavior must be extracted through a neutral package with contract tests, not by cross-client imports.
- Run the relevant client test and `npm run test:themes` for changes that affect both clients or shared infrastructure.
- Full reminder: `docs/dual-client.md`
