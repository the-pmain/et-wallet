# План работ

Порядок шагов из плана после аудита. Отмечено, что сделано и что осталось.
Реестр отложенных задач целиком — в [TECH_DEBT.md](TECH_DEBT.md),
обоснование приоритетов — в [AUDIT.md](AUDIT.md).

---

## Блок A — до реальных средств

| Шаг | Задача | Состояние |
| --- | --- | --- |
| 1 | **S-62** — запрос на развёртывание контракта исполнялся как перевод самому себе | **сделано** (`a06ae64`) |
| 2 | **S-63** — отвергать `eth_sign` и не заявлять его приложению | **сделано** (`a06ae64`) |
| 3 | **A-128** — экран отказа вместо белого экрана при сбое отрисовки | **сделано** (`a06ae64`) |

## Блок B — то, чем сегодня уводят деньги

| Шаг | Задача | Состояние |
| --- | --- | --- |
| 4 | **S-54** — список выданных разрешений и отзыв | **сделано** (`46a475a`) |
| 5 | **S-38** — встроенный список проверенных контрактов с пометкой в интерфейсе | **сделано** |

Про шаг 5: символ токена задаёт автор контракта, и отличить настоящий
USDC от поддельного можно только по адресу. Адреса брать живой проверкой,
а не по памяти.

## Блок C — надёжность и производительность

| Шаг | Задача | Состояние |
| --- | --- | --- |
| 6 | **A-124** — индекс истории транзакций | **сделано** |
| 7 | **A-129** — поведение в двух вкладках | **сделано** |
| 8 | **S-64** — шифрование пользовательских RPC-адресов | осталось |

Про шаг 6: измерено — 69.6 мс на 500 записей, и это платится каждые
12 секунд при слежении. Нужен индекс «адрес + сеть → хэши» либо индексы
IndexedDB с миграцией; замер до и после обязателен.

## Блок D — инженерная гигиена

| Шаг | Задача | Состояние |
| --- | --- | --- |
| 9 | **I-3** — CI на `npm run verify` и `npm audit` | осталось |
| 9а | **Внешний репозиторий** — проект существует в одном экземпляре на одном диске | осталось, требует действия владельца |
| 10 | **A-123** — общий `core/abi` вместо дублирования кодировки | осталось |
| 11 | **A-122** — разделить `WalletSession` на фасады | осталось |
| 12 | **A-127** — очистка кэшей при закрытии сессии | **сделано** (`46a475a`) |

Про шаг 9а: это единственный пункт, который не выполняется изнутри
проекта. Отказ диска стирает всю работу целиком — вероятнее любого
дефекта в коде.

## Блок E — развитие

Аппаратные кошельки, симуляция транзакций, QR для WalletConnect (A-90),
завершение интернационализации (A-85), пагинация истории (A-71),
список фишинговых доменов.
